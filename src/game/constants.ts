export const ROUND_COUNT_PRESETS = [10, 15, 20] as const;
export const DEFAULT_ROUND_COUNT = ROUND_COUNT_PRESETS[0];

export const MIN_PLAYERS = 2;
export const MAX_PLAYERS = 16;

export const DICE_PER_ROLL = 2;
export const DIE_MIN = 1;
export const DIE_MAX = 6;

export const BUST_TOTAL = 7;
export const EARLY_TURN_WINDOW = 3;
export const EARLY_SEVEN_BONUS = 70;
